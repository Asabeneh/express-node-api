<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partials/head %>
    <title>Node Express API</title>

  </head>

  <body>
    <div class="wrapper container-fluid">
      <header class="jumbotron text-center">
        <% include ../partials/navbar %>
        <h3>Edit Student</h3>
      </header>
      <main class="container">
        <form action="/api/v1/students/<%= student._id %>/edit" method="POST">
          <div class="form-row">
            <div class="form-group">
              <label for="name">Name:</label>
              <input type="text" id="name" class="form-control" name="name" value="<%= student.name %>" placeholder="Name"
                onfocus="var temp_value=this.value; this.value=''; this.value=temp_value" autofocus />
            
            </div>
            <div class="form-group">
              <label for="age">Age: </label>
              <input type="text" id="age" class="form-control" name="age" value="<%= student.age %>" placeholder="Age" />
            </div>
            <div class="form-group">
              <label for="country">Country: </label>
              <input type="text" id="country" class="form-control" name="country" value="<%= student.country %>"
                placeholder="Country" />
            </div>
            <div class="form-group">
              <label for="bio">Your bio: </label><br />
              <textarea name="bio" id="bio" class="form-control"><%= student.bio %></textarea>
            </div>
            <input type="submit" value="Update" class="btn btn-primary" />
            
          </div>
          
        </form>
      </main>
      <!-- footer -->
      
      <% include ../partials/footer %>
    </div>
    
    
  </body>
</html>
